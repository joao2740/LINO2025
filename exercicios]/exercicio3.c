#include <stdio.h>
#include <math.h>

int main(){
    //variaveis
    double A, B, C;
    double delta;
    double X1, X2;
    // ENTRADA
    printf("CALCULADORA DE RAIZES DE EQUAÇÃO DE SEGUNDO GRAU \n\n")

    printf("INSIRA O COEFICIENTE 'a': ");
    scanf("%lf", &a);
    printf("INSSIRA O COEFICIENTE 'b': ");
    scanf("%lf", &c);
    printf("INSIRA O COEFICIENTE 'c': ");
    scanf("%lf", &c);
    printf("\n");
    //processar dados 
    delta = (B*B) - (4 * A * C);

    //VER OS DADOS
    if (delta >= 0) {
        if (delta == 0) {

            X1 = -B / (2 * a);
            printf("A EQUAÇÃO POSSUI UMA RAIZ REAL (OU DUAS IGUAIS): %.2F\n", X1);
            }else{
                X1 =      
        
        /* code */
    }
    
}